# 開発ガイド

このファイルは、開発時の共通ルールと、作業内容に応じて参照・更新する文書を案内する。

## 開発ルール

- 個人利用のプロジェクトにつき、**後方互換性は考慮してはいけない。**
- シェルスクリプトはグローバル変数を利用しない。かならず関数の返値を利用すること。
- コメントは日本語、エラー文は英語。
- コミットメッセージは規模に応じて「メインメッセージ」+「詳細3行まで」とする。
  - なるべく背景・意図を含めること。
- コミット時は可能なら fast forward で行う。
- 関数や定数には責務と内容を簡単にJSDocでコメント記述する（基本アノテーションなし）。
- application のユースケース実装は `*.usecase.ts`、domain のサービス実装は `*.service.ts` とする。
- 複数関数を1つの責務として公開する場合は `*Usecase` または `*Service` の object export を基本とする。
- テストケースはプロダクションコードのそばに置き、命名は `*.spec.ts` とする。
- コード変更後は基本的に `npm run check:all` をパスさせること。
- knip 実行を指示された時だけ `npm run unused` で確認すること。

## ドキュメント

### `README.md`

プロジェクトの入口。概要、初期セットアップ、設定、開発開始手順を記載する。

- 参照するタイミング: このリポジトリでの作業を始めるとき。
- 変更するタイミング: プロジェクトの目的・概要、前提環境、初期セットアップ、設定項目、基本コマンドが変わったとき。

### `docs/design.md`

Worktree Deck が提供する価値、システムの主要な責務、外部システムとの境界を記載する。

- 参照するタイミング: 機能の目的、システム全体で担う範囲、主要要素の関係、外部依存を確認するとき。
- 変更するタイミング: 提供価値、システム責務、外部システムとの境界や重要な制約が変わったとき。

### `docs/architecture.md`

Worktree Deck の実装構造、層ごとの責務、依存と組み立てのルール、データの所有境界を記載する。

- 参照するタイミング: 実装の責務、配置する層、依存方向、設定・保存先、provider 境界を判断するとき。
- 変更するタイミング: 実装構造、層責務、依存ルール、データの所有境界、セッション処理の境界が変わったとき。

### `docs/domain.md`

worktree-deck で継続して使う主要なドメイン用語と、その意味・関係を記載する。

- 参照するタイミング: 要件、設計、実装、UI 表示で使う言葉の意味を確認するとき。
- 変更するタイミング: 継続的に使う概念を追加・変更・削除するとき、または既存用語の意味や関係が変わったとき。設定項目や実装上の識別値は含めない。

内容が複数の文書に当てはまる場合は、詳細をその内容の source of truth となる文書またはコードに記載し、他の文書からリンクする。正確な現在値や実装の振る舞いは、コード、設定、スキーマ、テストを優先する。
